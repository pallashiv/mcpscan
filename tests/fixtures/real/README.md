Real `tools/list` output captured on 2026-09-21 with the MCP Inspector CLI from Anthropic's
reference servers (`@modelcontextprotocol/server-everything`, `-filesystem`, `-memory`,
`-sequential-thinking`; MIT licensed). They are legitimate servers, so they guard against
false positives: see `tests/test_real_manifests.py`.
